# imports
import os

# global variables
#os.environ['AWS_PROFILE'] = ''

def handler(event, context=None):
    """The entrypoint of your lambda app. When AWS runs your app, this will be the method it calls"""

    return